# Triage Labels

This repo does not use a formal triage state machine. Issues tracked under `.scratch/` are assumed to be ready for work unless noted otherwise in the file itself.

If a state needs to be communicated, note it in the issue markdown (e.g., "Status: blocked on X").
